# Dependencies
node_modules/
.next/
.nuxt/
.svelte-kit/

# Environment
.env
.env.local
.env.*.local

# Output / build
dist/
build/
out/
# …but the framework export unit has a legit folder named build/ (Build stage)
!framework/build/

# Test output
testing/playwright/reports/
test-results/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*

# Generated files (regenerated from templates)
design-system/*/tokens/tokens.css
launcher/.vite/deps

# Machine-local launcher state (written by idea-intake)
# .idea-sessions/ holds staged mid-interview transcripts — the user's raw
# business ideas — so it must never reach GitHub.
project-dir.txt
.idea-sessions/

# Codespaces setup reference (contains provider secrets — never commit)
.codespace-secrets.txt

# Launcher — runtime database (regenerated by `npm run db:reset && npm run db:seed`)
launcher/data/