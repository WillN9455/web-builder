# Idea Hub — App Plan

> **Archived.** The changelog-layered v5.2 plan is preserved verbatim as **`plan.archived.md`** for history.
>
> The canonical site map is now **`sitemap.md`** — one page contract per screen/tab, in route order, with no changelog layers. `sitemap.md` folds in everything still valid from this file (stage model, per-project menu, intake flow, Jira/build, data model, stack, accessibility/state coverage) and applies the restructure decisions from `restructure-brief.md`:
>
> - 10-tab per-project sidebar (Sprint split from Build; Overview collapses Screens 3/4/5).
> - Project Background is the gate (two-step, one-shot unlock).
> - Every stage tab has a status half + an editable rules half that writes back to the project folder.
> - Stale labels reconciled: `PRD` → `Requirements`, `Shipped` → `Deployed`, `Mark Completed ✓` → `Approve ✓` / status `Approved`, Build badge `Jira` → ticket count.
>
> Read `sitemap.md` first. This file is kept only so older links and history remain reachable.