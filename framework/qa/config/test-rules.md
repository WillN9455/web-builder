# Test Rules (rules in force)

> Written by the Idea Hub **QA tab → Rules half**. The QA Agent reads this at
> task pickup. Every user edit on the QA Rules surface lands here.

## Draft sections

- **Test framework config** — runner, coverage thresholds, environment
- **Rules in force** — flake policy, blocking policy, evidence rules (screenshots first-class)
- **Per-agent QA guidelines** — overrides for the QA Agent and Reviewer Agent

> **v1 draft stub** — sections to be filled from the QA · Rules mockup
> (qa-tab.html) when the launcher wires write-back.