# Build templates

Starter scaffolds the Code Agents build from. Stack selection in
`../config/config-rules.md` picks the template; `tech-decision-brief.md` is
the definitive per-project call.

**v1 draft:** contents migrate from `code-builder/templates/` (nextjs-starter
and the vue-nuxt / svelte-kit variants listed in `init-frame.js`) after the
debate converges.