# Code Agents (C1–C3) — loadouts & steering

> Written by the Idea Hub **Agents tab editor**. Each Code Agent reads its
> section here at task pickup. All user steering for C1–C3 lives in this file.
>
> **v1 choice:** one file for the code-agent family rather than three files —
> they share a role contract and most steering applies to all three. **Debate
> flag:** Solution Architect may argue for one-file-per-agent so the Agents
> tab can clone/remove agents without touching siblings.

## Shared loadout (applies to C1, C2, C3)
- Stage: Build
- Model / effort: *(edited on Agents tab — values written here)*
- Trigger: story assigned on the Sprint board
- Skills in force: `../skills/coding-guidelines.md`, `../skills/code-quality.md`, `../skills/security.md`, `../skills/feature-fidelity.md`
- Scaffold from: `../templates/` (per `../config/config-rules.md` stack selection)

## C1 — Code Agent 1
- Skills in force (overrides): —
- Adversarial review: critiqued by Reviewer Agent
- Steering notes (user overrides): *(empty)*

## C2 — Code Agent 2
- Skills in force (overrides): —
- Adversarial review: critiqued by Reviewer Agent
- Steering notes (user overrides): *(empty)*

## C3 — Code Agent 3
- Skills in force (overrides): —
- Adversarial review: critiqued by Reviewer Agent
- Steering notes (user overrides): *(empty)*